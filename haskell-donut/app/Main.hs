module Main(main) where
{-
import Graphics.Gloss
-- hackage has a bunch of juicy details on this nice and simple library

-- | the wrapper function for everything I could possibly need
animate :: Display
        -> Color
        -> (Float -> Picture) -- ^ function which takes the time elapsed and outputs a Picture
        -> IO ()

-- data Color   = ... | 
background :: Color
color = White

-}

main = putStrLn "Are we there yet?"
